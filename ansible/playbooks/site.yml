---
- name: Bootstrap OS for all servers
  hosts: all
  become: true
  gather_facts: true
  roles:
    - role: common

- name: Configure monitoring stack
  hosts: monitor
  become: true
  gather_facts: true
  # 役割の順序: docker / nginx でホスト前提を整え、app がリポジトリを同期して compose を起動、
  # その上で monitoring が環境別設定（Alertmanager）を上書きしてランタイム構文検証を行う。
  # backup は compose volume の名前が確定した後で systemd timer を導入する。
  roles:
    - role: docker
    - role: nginx
    - role: app
    - role: monitoring
    - role: backup

- name: Verify monitoring stack is healthy
  ansible.builtin.import_playbook: verify.yml

---
galaxy_info:
  role_name: common_windows
  author: server-monitor
  description: >-
    Windows Server baseline for docs/build-package-windows (Firewall rules,
    IIS site, windows_exporter, backup policy). Assumes WinRM(HTTPS) is
    already reachable; the initial OS naming and WinRM HTTPS listener
    bootstrap in 05-build-procedure.md 0-2節 must still be done from the
    hypervisor console first (see role README note in tasks/main.yml).
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Windows
      versions:
        - "2022"
  galaxy_tags:
    - windows
    - monitoring
dependencies: []

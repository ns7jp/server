# SLO 月次レビュー: 2026-05

SLO / SLI / エラーバジェットの構成コードを導入した時点のレビュー用サンプル。
本ファイルには稼働環境から取得した計測データや実施会議の証跡はなく、
レビュー手順と記載形式のテンプレートとして扱う。

## 1. 集計サマリー

| 項目 | 値 | 出典 |
| --- | --- | --- |
| 30 日可用性 | (初回計測中) | Grafana `slo-overview` |
| エラーバジェット消費率 | — | recording rule 未蓄積 |
| エラーバジェット残量 | 未測定 | 実行証跡なし |
| /healthz 28 日 p95 | (初回計測中) | — |
| Fast burn 発火回数 | 未測定 | 実行証跡なし |
| Slow burn 発火回数 | 未測定 | 実行証跡なし |

## 2. インシデント

未測定。実運用または演習のログはまだ収録していない。

## 3. 計画停止

| 日時 (JST) | 時間 | 内容 | バジェット控除 |
| --- | --- | --- | --- |
| 未実施 | - | 構成コードのみ整備済み | - |

## 4. 判断

| 項目 | 状態 |
| --- | --- |
| SLO 達成 | 判定保留（30 日分のデータが未蓄積） |
| バジェット消費区分 | 判定保留（実測なし） |
| 新機能リリース | 実稼働時に判断 |

## 5. 改善計画

実行証跡が未収録であるため、まず D-1 または稼働環境での観測結果を採録する。

## 6. アクションアイテム

- [ ] 翌月（2026-06）レビューに向けて、blackbox-exporter の 30 日データが蓄積される
      ことを確認する。
- [ ] 月次レビュー会の運用を決める（実施日、参加者、所要時間）。
- [ ] Slack 通知に `runbook_url` がリンクとして展開されるか確認する。
- [ ] 計画停止を SLI 計算から除外する手順を仕組み化する（現状は手動記録のみ）。

---

参考: [docs/slo.md](../../slo.md)
